# ========================================
# WORKFLOWGHAZAL - BACKEND CONFIGURATION
# ========================================

# ========================================
# DATABASE CONFIGURATION
# ========================================
# Format: postgresql://[user]:[password]@[host]:[port]/[database]?schema=[schema]
DATABASE_URL="postgresql://postgres:your_password@postgres_db_ghazal_workflow:5432/workflow?schema=public"
POSTGRES_USER=postgres
POSTGRES_PASSWORD=your_password
POSTGRES_DB=workflow
POSTGRES_HOST=postgres_db_ghazal_workflow
POSTGRES_PORT=5432
DATABASE_SCHEMA=public


# ========================================
# SERVER CONFIGURATION
# ========================================
# Port sur lequel le serveur backend écoute (inside Docker container)
PORT=3000
NESTJS_PORT=3010
# Environnement d'exécution (development | production | test)
NODE_ENV=development

# ========================================
# JWT CONFIGURATION
# ========================================
# Secret pour signer les tokens JWT
# IMPORTANT: Générez un secret fort et unique avec: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
# NE JAMAIS commiter le vrai secret dans git!
JWT_SECRET=your_super_secret_jwt_key_here
# Durée de validité des tokens JWT (exemples: 1h, 24h, 7d, 30d)
JWT_EXPIRES_IN=24h

# ========================================
# CORS CONFIGURATION
# ========================================
# URL du frontend autorisée pour les requêtes CORS
# En production, utilisez plusieurs URLs séparées par des virgules
# Exemple production: https://app.ghazal.com,https://www.ghazal.com
CORS_ORIGINS=https://www.ghazal.dz,https://ghazal.dz,http://localhost:5173

# ========================================
# API CONFIGURATION
# ========================================
# Préfixe global pour toutes les routes API
API_PREFIX=apiworkflow

# ========================================
# WEBSOCKET CONFIGURATION
# ========================================
# WebSocket path (must match frontend and nginx)
WS_PATH=/apiworkflow/socket.io

# ========================================
# FRONTEND URL (for reference)
# ========================================
FRONTEND_URL=https://www.ghazal.dz